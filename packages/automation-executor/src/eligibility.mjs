export function assessEligibility({ chainId, expectedChainId, asset, expectedAsset, balance, threshold }) { if (chainId !== expectedChainId) return { eligible: false, reason: "wrong_chain" }; if (asset.toLowerCase() !== expectedAsset.toLowerCase()) return { eligible: false, reason: "wrong_token" }; if (balance < threshold) return { eligible: false, reason: "below_threshold" }; return { eligible: true, reason: "eligible" }; }
